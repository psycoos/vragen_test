<%= form_for(@saved_result) do |f| %>
  <% if @saved_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saved_result.errors.count, "error") %> prohibited this saved_result from being saved:</h2>

      <ul>
      <% @saved_result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
